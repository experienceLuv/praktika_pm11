<Window x:Class="KeeperProWpf.Department.Windows.BlackListReasonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Черный список"
        Height="300"
        Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Укажите причину добавления в черный список"/>

        <TextBox x:Name="ReasonTextBox"
                 Grid.Row="1"
                 Margin="0,10,0,10"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 MaxLength="5000"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Width="120" Margin="0,0,10,0" Click="SaveButton_Click"/>
            <Button Content="Отмена" Width="120" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>